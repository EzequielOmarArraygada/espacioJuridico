<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Página No Encontrada</title>
    <link rel="stylesheet" href="/css/style.css">
    <style>
        body {
            background-color: #f7f9fc;
            font-family: 'Arial', sans-serif;
            text-align: center;
        }

        .container {
            max-width: 600px;
            margin: 0 auto;
        }

        .error-title {
            font-size: 6rem;
            color: #ff6f61;
            margin-bottom: 20px;
        }

        .error-message {
            font-size: 1.5rem;
            color: #333;
        }

        .home-link {
            display: inline-block;
            margin-top: 20px;
            padding: 10px 20px;
            background-color: #ff6f61;
            color: white;
            text-decoration: none;
            border-radius: 5px;
            transition: background-color 0.3s ease;
        }

        .home-link:hover {
            background-color: #ff3d2e;
        }

        .rocket {
            width: 150px;
            animation: fly 2s ease-in-out infinite;
        }

        @keyframes fly {
            0% {
                transform: translateY(0);
            }
            50% {
                transform: translateY(-20px);
            }
            100% {
                transform: translateY(0);
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <img src="/assets/rocket.png" alt="Rocket" class="rocket">
        <h1 class="error-title">404</h1>
        <p class="error-message">Oops... No encontramos la página que buscas.</p>
        <a href="/" class="home-link">Volver al Inicio</a>
    </div>
</body>
</html>
